# Pinned deliberately. FastMCP moved out of mcp.server.fastmcp in a later SDK
# release, so an unpinned ">=1.9.0" resolves to something where the import in
# immich_mcp.py does not exist. 1.27.0 is verified working.
mcp==1.27.0
httpx>=0.27
pillow>=10.3
uvicorn>=0.30
starlette>=0.37
pydantic>=2.7
